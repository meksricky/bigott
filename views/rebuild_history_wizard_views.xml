<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>

    <!-- Wizard view -->
    <record id="view_rebuild_client_history_wizard_form" model="ir.ui.view">
      <field name="name">rebuild.client.history.wizard.form</field>
      <field name="model">rebuild.client.history.wizard</field>
      <field name="arch" type="xml">
        <form string="Rebuild Client History">
          <group>
            <field name="date_from"/>
            <field name="date_to"/>
          </group>
          <footer>
            <button string="Run" type="object" name="action_run" class="btn-primary"/>
            <button string="Cancel" class="btn-secondary" special="cancel"/>
          </footer>
        </form>
      </field>
    </record>

    <!-- Action to open the wizard -->
    <record id="action_open_rebuild_client_history_wizard" model="ir.actions.act_window">
      <field name="name">Rebuild from Sales</field>
      <field name="res_model">rebuild.client.history.wizard</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
    </record>

  </data>
</odoo>
